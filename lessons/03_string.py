# 第 3 课：字符串
#
# 怎么用这个文件：
#   1. 先通读一遍，别急着改
#   2. 补完每个【练习】（在它下面直接写代码）
#   3. 看到【猜】先把猜测写成注释，再运行验证
#   4. 全做完后关掉本文件，新建 03_rewrite.py 从零重写
#
# 卡住了就来问我。这个文件里没有答案。


# ============================================================
# 1. 索引：取单个字符
# ============================================================
# C 里字符串是 char 数组，s[0] 取第一个字符。
# Python 也从 0 开始数——但多了一个 C 完全没有的东西：负索引。

s = "Python"
print("s[-1] =", s[-1])      # P
print("s[-2] =", s[-2])      # y

# 【猜 1】下面两个结果是什么？先猜，写在注释里，再运行。
#         s[-1]     我猜：n
#         s[-2]     我猜：o
# 提示：负索引是「从末尾倒着数」，-1 表示最后一个。

# 【练习 2】用负索引打印 s 的最后一个字符

# 【练习 3】故意越界：写一行 print(s[100])，运行。
#           把报错类型抄在这行注释后面：string index out of range
#           报错类型是：IndexError
#           想想：这个报错和之前见的 NameError / ValueError 有什么不同？
#这个是索引错误
#print(s[100])

# ============================================================
# 2. 切片 ⭐ 本节课最重要的东西
# ============================================================
# 语法：s[起点:终点]
# ⚠️ 注意：包含起点，不包含终点（叫「左闭右开」）

t = "0123456789"
    # 234   ← 包含 2，不包含 5

# 【猜 4】下面四个分别是什么？先全部猜完，再一次性运行验证。
#         t[:3]        我猜：012
#         t[7:]        我猜：789
#         t[:]         我猜：0123456789
#         t[2:5:2]     我猜：24← 第三个数字叫「步长」
print("t[:3] =", t[:3])
print("t[7:] =", t[7:])
print("t[:]  =", t[:])
print("t[2:5:2] =", t[2:5:2])
# 【练习 5】用切片取出 t 的偶数位，结果是 "02468"
print("t[0::2] =",t[0::2])
# 【猜 6】t[::-1] 是什么？先猜再运行。
#         我猜：9876543210
#         这个写法以后会经常用——它是 Python 里「反转字符串」的标准写法。
print("t[::-1] =", t[::-1])
# 【练习 7】用切片取出 s（"Python"）中间的 "tho" 三个字母
print(s[2:5])

# ============================================================
# 3. 字符串是不可变的 ⭐ 和 C 很不一样
# ============================================================
# C 里可以改：char s[] = "abc"; s[0] = 'x';   合法
# Python 里不行。

u = "abc"

# 【练习 8】试着改一个字符：写 u[0] = "x"，运行，读报错。
#           报错类型是：
#           想想：那我想「改」字符串该怎么办？
#           （提示：只能造一个新的，不能改原来那个）

# 【练习 9】把 u 的首字母变成大写，结果存进新变量 v。
#           打印 v 和 u，观察 u 有没有被改变。
#           提示：字符串有个 .upper()、也有 .capitalize()


# ============================================================
# 4. 常用方法
# ============================================================
# 方法 = 挂在「值」上的函数，用点号调用：s.方法名()
# 这和 C 完全不同——C 里是 strlen(s)，Python 里是 len(s) 或 s.方法()

w = "  Hello, Python World  "

# 【练习 10】把下面每个的结果打印出来，一行一个：
#            len(w)                      有几个字符
#            w.strip()                   去掉首尾空白
#            w.lower()                   全小写
#            w.upper()                   全大写
#            w.strip().split(",")        按逗号切开——得到什么类型？
#            w.replace("World", "朋友")   替换
#            "Python" in w               w 里有没有 "Python"
#
#            ⚠️ 注意 len(w) 和 len(w.strip()) 不一样。想想为什么。

# 【练习 11】join 是 split 的反操作。先猜结果，再运行：
#            "-".join(["a", "b", "c"])


# ============================================================
# 5. f-string 进阶：格式化
# ============================================================
pi = 3.14159265
n = 42

# 【练习 12】下面每个都先猜长什么样，再运行：
#            f"{pi:.2f}"      保留 2 位小数
#            f"{pi:.0f}"      不要小数
#            f"{n:05d}"       补零到 5 位
#            f"{n:>10}"       右对齐，占 10 格
#            f"{n:<10}"       左对齐，占 10 格
#
#            ⚠️ 记住笔记里的 M4：**要控制小数显示位数，用格式化，别去改浮点数本身**


# ============================================================
# 6. 综合练习：文本统计器
# ============================================================
text = "the quick brown fox jumps over the lazy dog the fox"

# 【练习 13】对上面这段文字做统计。每一步先想清楚用哪个方法，再写。
#
#   1. 打印总字符数
#   2. 转成小写，再按空格切成单词列表
#   3. 打印单词总数
#   4. 用 .count() 打印 "fox" 出现了几次
#      （想统计「每个词各出现几次」需要字典，第 5 课再讲）
#   5. 打印反转后的整段文字——用你刚学的切片


# ============================================================
# 自检：全做完之后再打开这一节
# ============================================================
# 新建一个空文件 03_rewrite.py，关掉本文件，不看任何资料写出：
#
#   1. 给定 s = "abcdefg"，用切片取出 "cde"
#   2. 用切片把 s 反转并打印
#   3. 给一句带首尾空格的话，去掉首尾空格、按空格切成列表，打印列表和它的长度
#   4. 把 3.14159 格式化成保留 3 位小数并打印
#
# 四题都写出来了 → 第 3 课过关，告诉我，我批改并推送。
# 有任何一题写不出来 → 不要往下走，把那题重写一遍。
