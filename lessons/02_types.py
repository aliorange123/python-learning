# 第 2 课：变量与类型
#
# 怎么用这个文件：
#   1. 先通读一遍，别急着改
#   2. 把每个【练习】下面的 TODO 补完
#   3. 每补完一个就运行一次：python lessons\02_types.py
#   4. 看到【猜】字样的，先把猜测写成注释，再运行验证
#   5. 全做完后关掉这个文件，新建一个 02_rewrite.py 从零重写一遍
#
# 卡住了就来问我。这个文件里没有答案，别找。


# ============================================================
# 1. 动态类型：最大的思维转变
# ============================================================
# C 里你写：int x = 1;
# Python 里不用声明类型，直接赋值。类型是「值」的属性，不是「变量」的。

x = True
print("x =", x)
print("x 的类型是:", type(x))

# 【练习 1】把上面 x = 1 依次改成 3.14、"hello"、True
#          每改一次运行一下，观察 type(x) 的输出变化。
#          然后问自己：同一个变量 x，为什么能装不同的类型？


# ============================================================
# 2. 整数没有上限
# ============================================================
# C 的 int 大约到 21 亿就溢出了。Python 的整数可以无限大。

print("2 的 100 次方 =", 2 ** 100)

# 【练习 2】算出 100 的阶乘，并打印它有多少位数字。
#          提示：import math 之后可以用 math.factorial(100)
#          提示：数字的位数 = len(str(那个数))
import math
x = math.factorial(100)
y = len(str(x))
print (x)
print (y)

# ============================================================
# 3. 浮点数的坑（这个必须亲手撞一次）
# ============================================================

# 【猜 3】先别运行！看下面这行代码，猜猜它会打印什么：
#             print("0.1 + 0.2 =", 0.1 + 0.2)
#         把猜测写在下面这行注释里：
#           我猜输出是：0.3
#         写完之后，把再下面那行开头的 # 去掉，运行验证。
#         如果和猜的不一样，先别急着查资料，想想这两个问题：
#           1/3 在十进制里是 0.3333... 永远写不完，
#           那 0.1 在二进制里写不写得完？
#           计算机存不下无限长的小数，那它会怎么办？

print("0.1 + 0.2 =", 0.1 + 0.2)


# ============================================================
# 4. 字符串
# ============================================================

name = "aliorange123"
# 【练习 4】用 type() 打印 name 的类型
print("name的类型=",type(name))
# 【练习 5】用 f-string 打印一句自我介绍，包含 name 和你自己的年龄。
#          提示：f"我是 {name}，今年 {年龄} 岁"
name = "aliorange123"
age = 124
print(f"我是{name}，今年{age}岁")

# ============================================================
# 5. 布尔值：其实它就是整数
# ============================================================

# 【猜 6】下面两个表达式的结果分别是什么？先写猜测，再运行。
 #       True == 1       我猜：1
  #      type(True)      我猜：bool
  #     True + True     我猜：2
 #               然后想：如果 True 就是 1，那 Python 为什么还要单独有个 bool？
print("True == 1 的结果是:", True == 1)
print("type(True)的结果是:",type(True))
print("True + True的结果是:",True + True)


# ============================================================
# 6. None：不是 NULL，也不是 0
# ============================================================

result = None
print("result 是:", result)

# 【猜 7】下面三个表达式的结果？先猜再运行。
#         None == 0        我猜：False
#         None == ""       我猜：False
#         None == None     我猜：True
#         然后想：C 的 NULL 和 Python 的 None，最大的区别是什么？
#         （提示：C 里对 NULL 做运算会崩，Python 里呢？试试 None + 1）
print(x,None == 0)
print(y,None == "")
print(None == None)
print(0 == "")
print(0 == [])
print("" == [])
print(bool(0), bool(""), bool([]), bool(None))
# ============================================================
# 7. 类型转换：新手最常卡的地方
# ============================================================

# 【练习 8】"123" 是字符串。把它变成整数 123，打印结果和它的类型。
#          提示：int("123")
a = int("123")
print(a)
print(type(a))
# 【练习 9】反过来，把整数 123 变成字符串 "123"。
a = 123
b = str(a)
print(b,type(b))
# 【猜 10】int("3.14") 会成功吗？先猜再运行。
#          我猜不会成功，因为int是整形 3.14是浮点型
#          如果失败了：
#            - 报错信息里哪个词你没见过？查一下它什么意思
#            - 想让它成功，该怎么改？
#          如果成功了：那你运气不错，但我还是建议你查一下报错信息长什么样 :)
print(int(float("3.14")))
# 【练习 11】把字符串 "3.14" 变成浮点数，再加 1，打印结果。
a = float("3.14")
a = a + 1
print(a)
# ============================================================
# 自检：全做完之后再打开这一节
# ============================================================
# 新建一个空文件 02_rewrite.py，关掉本文件，不看任何资料写出：
#
#   1. 两个变量分别存你的名字和年龄，用 f-string 打印一句介绍
#   2. 把字符串 "42" 转成整数，加 1，打印结果和它的类型
#   3. 用注释回答：为什么 0.1 + 0.2 不等于 0.3？
#
# 三题都写出来了 → 第 2 课过关，告诉我，我批改并推送到 GitHub。
# 有任何一题写不出来 → 不要往下走，把那题的代码重写一遍。
