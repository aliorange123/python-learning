# Python 学习笔记

> **给我自己看的，不是教程。** 教程网上有的是。
> 这里只记三样东西：
> 1. **我踩过的坑** —— 网上查不到，只有我有
> 2. **和 C 不一样的地方** —— 我最大的障碍来源
> 3. **我原本想错的地方** —— 最容易再错一次的
>
> 复习时优先看「🕳️ 踩坑清单」和「🎯 自测题」，别从头读到尾。

**进度**：第 2 课 / 共 12 课 ｜ **最后更新**：2026-09-24

---

## 📌 速查：容易忘的东西

随时补充。复习时先扫这一栏。

| 我想要 | 怎么写 |
|---|---|
| 数一个数有几位 | `len(str(x))` |
| 字符串 → 整数 | `int("123")` → `123` |
| 整数 → 字符串 | `str(123)` → `"123"` |
| 字符串 → 小数 | `float("3.14")` → `3.14` |
| 小数 → 整数（**直接砍掉**） | `int(3.99)` → `3` |
| 小数 → 整数（四舍五入） | `round(3.99)` → `4` |
| 看一个值是什么类型 | `type(x)` |
| 用 math 里的函数 | 文件顶部先 `import math`，再写 `math.factorial(100)` |
| 比较两个浮点数 | `abs(a - b) < 1e-9`　**绝不用 `==`** |
| 打印多个东西 | `print(a, b, c)` 或 `print(f"{a} {b}")` |

---

## 🕳️ 踩坑清单

每个坑都写清楚：**什么现象 → 为什么 → 怎么避免**

### P1 · 改了文件，运行却没变化

- **现象**：明明改了代码，运行还是老结果，**而且不报错**（最坑的一种）
- **原因**：没保存。VS Code 里看到的是**内存**里的内容，`python` 读的是**硬盘**上的文件
- **避免**：**运行前先看标签页有没有 `●` 圆点**
- **踩了 3 次**：09-22、09-23、09-24

### P2 · `IndentationError: unexpected indent`

- **现象**：报「意外的缩进」
- **两种来源**：
  1. 删 `#` 注释时，把 `#` 后面的**空格**留下了（`# print(x)` → 删掉 `#` 剩 ` print(x)`）
  2. 新写的行被 VS Code 自动缩进带偏了
- **避免**：**打开 `查看 → 显示空白字符`**，行首空格会变成小圆点，一眼可见
- **踩了 4 次**——同一个根因反复中招，说明「看不见空格」是真问题

### P3 · `NameError: name 'xxx' is not defined`

- **现象**：报「某个名字没定义」
- **三种原因**：
  1. **忘了 import**：用了 `math.factorial()` 却没写 `import math`
  2. **拼写错**：定义了 `fangxiang`，写成 `fangxian`
  3. **根本没定义**：写 `print(z)`，但整个文件没有 `z = ...`
- **避免**：报错会直接告诉你是哪个名字。**先查拼写，再查有没有定义/导入**
- **口诀**：**用之前，先要有**

### P4 · 报错里的「建议」不一定对

| 报错里的建议 | 实际 |
|---|---|
| `Perhaps you forgot a comma?` | ❌ **猜错了**，真实原因是少了个引号 |
| `Did you forget to import 'math'?` | ✅ 猜对了 |

- **结论**：那些 `Did you mean...?` / `Perhaps you forgot...?` **只是猜测**，当线索看
- **真正可靠的是**：**行号 + 箭头 `^` 指向的位置**

### P5 · 文件建到了错误的目录

- **现象**：`code lessons\01_rewrite.py`（在 `C:\Users\10486>` 下执行），文件跑到 `C:\Users\10486\lessons\` 去了
- **原因**：**相对路径以「当前所在目录」为基准**，不是以项目目录为基准
- **避免**：
  - 先 `cd D:\code\python-learning`，再用 `code` 打开文件
  - 或者更省事：`code D:\code\python-learning` **打开整个文件夹**，从左边文件树里点文件
  - 打开后看**面包屑**确认：`D: > code > python-learning > lessons > ...`

### P6 · PowerShell 里的引号会吃掉参数

- **现象**：`git commit -m "... "3.14" ..."` 报 `error: pathspec '3.14' did not match any file(s)`
- **原因**：**PowerShell 用反引号 `` ` `` 转义，不认反斜杠 `\"`**。提交信息里的 `"` 被当成了字符串结束
- **避免**：**PowerShell 里传含引号的参数，用单引号包起来** `'...'`
- 这个坑在 `git config` 时也遇到过一次（09-22）

---

## 🔄 C 和 Python 不一样的地方

**这是我最大的障碍来源，越攒越多。**

| | C | Python |
|---|---|---|
| 声明类型 | `int x = 1;` | 不用声明，直接 `x = 1` |
| **类型的归属** | 属于**变量**（格子的属性） | 属于**值**（对象的属性）；变量只是名字 |
| 代码块 | `{}` | **缩进**（行首空格是语法！） |
| 语句结束 | `;` | 换行 |
| 引入库 | `#include <math.h>` | `import math` |
| 调库函数 | `pow(2, 10)` | `math.factorial(100)`（要写 `math.` 前缀） |
| 整数上限 | ~21 亿，会溢出 | **无上限**（`2**100`、`100!` 都能算） |
| 空值 | `NULL`（解引用直接崩） | `None`（用错了报一个能读懂的错） |
| 错误处理 | 错误码 / `errno` | `try` / `except` |
| 相等判断 | `==` | `==`（`=` 是赋值，**两回事**） |
| 注释 | `//`、`/* */` | `#` |

---

## 🧠 我原本想错的地方

**最容易再错一次的，重点看这一节。**

### M1 · 以为 `print` 的结果能反映类型

```python
print(123)                      # 123
print("123")                    # 123        ← 一模一样！
print(type(123), type("123"))   # <class 'int'> <class 'str'>   ← 只有它能区分
```

**教训：验证类型转换有没有成功，必须打印 `type()`，不能只看输出。**

### M2 · 以为 `==` 会返回 `1` / `0`（后来又以为 `True == 1` 是 `False`）

```python
True == 1        # True    ← 不是 1，也不是 False
None == None     # True    ← 不是 1
```

**`==` 永远返回 `True` 或 `False`。** 这题我错了**三次**（09-23、09-24、09-25）。

**✅ 想通的标志**（我自己总结的话，09-25）：

> **`True == 1` 是「判断」（`==`），判断永远返回 `True`/`False`；
> `True + True` 是「运算」（`+`），算术语境里 `bool` 退化成 `1`/`0`。**

```python
True == 1        # True    ← 判断 → 返回 bool
True + True      # 2       ← 运算 → 1 + 1
```

### M3 · 以为 `None == 0` 是 True

```python
None == 0        # False    ← 它们不相等
bool(None)       # False    ← 但 None 确实是「假值」
```

**亲手跑出来的证据**（09-25）：

```
0 == ""       →  False
0 == []       →  False
None == 0     →  False
False == 0    →  True      ← 五个假值里，只有它是 True
```

| 值 | 是假值吗 | 等于 0 吗 | 为什么 |
|---|---|---|---|
| `0` | ✅ | ✅ | 它**就是** 0 |
| `False` | ✅ | ✅ | 它**就是** 0（bool 是 int 的子类） |
| `""` | ✅ | ❌ | 只是「碰巧也是假的」 |
| `[]` | ✅ | ❌ | 同上 |
| `None` | ✅ | ❌ | 同上 |

**结论：「是假值」和「等于 0」是两件独立的事。**
`False == 0` 为 True，是因为它俩**本来就是同一个东西**；
`None == 0` 为 False，是因为它俩**只是碰巧都为假**。

### M4 · 以为 `0.1 + 0.2 == 0.3`

```python
0.1 + 0.2            # 0.30000000000000004
3.14 + 1             # 4.140000000000001     ← 更狠：两个「正常」小数也不等
(3.14 + 1) == 4.14   # False
```

**原因**：`0.1` 在二进制里是无限循环小数（就像 `1/3` 在十进制里写不完），
存的时候被**截断**成最接近的值 → 两个都有误差的数相加 → 误差显现。

**规则：比较浮点数永远不要用 `==`，用 `abs(a - b) < 1e-9`。**

### M5 · 以为 `int()` 是四舍五入

```python
int(3.99)      # 3    ← 直接砍掉小数部分
round(3.99)    # 4    ← 要四舍五入得用 round
```

算钱、算人数时用错会出事。

### M6 · 以为变量有类型

```python
x = 1
x = "hello"    # 合法！
x = True       # 也合法
```

**变量没有类型，值才有。** `x` 只是个名字，随时可以指向别的对象。

### M7 · 把 `f{...}` 当成 f-string

```python
f{我的名字是name}          # ❌ SyntaxError
f"我的名字是{name}"        # ✅
```

`f` 贴在**引号**前面，**字符串的引号不能少**。
`{}` 的作用是「在字符串**里面**标出变量」，**不是**「包住整个东西」。

正确结构：

```
f "  我的名字是 {name}  "
│ │              └── 变量标记（在字符串内部）
│ └── 字符串的引号（必须有）
└── f 标记（贴在引号前）
```

### M8 · 把注释当成代码去「取消注释」

练习题里 `#` 开头的行有两种：

- **真代码被注释掉了** → 取消注释就能跑
- **说明文字** → 取消注释**不会**让它变成代码，照样报错

**先看清楚那是代码还是说明。**

### M9 · 把「说明文字」也复制进了代码

一次是把 `#` 注释取消掉当代码用（见 M8），一次是把聊天里的 **`→` 箭头**也抄进了代码：

```
SyntaxError: invalid character '→' (U+2192)
```

那个 `→` 是我标注「输出是」用的，**不是代码**。

> 💡 报错里的 `(U+2192)` 是这个字符的 Unicode 编号——以后从网页复制代码混进怪字符，报错里会直接告诉你。

**避免**：抄代码时**只抄 `print(...)`、`x = ...` 这种真正是代码的部分**。
更保险是**手打**——手打时脑子会过一遍。

### M10 · 以为 `len()` 能直接用在整数上

```python
x = 12345
len(x)            # ❌ TypeError: object of type 'int' has no len()
len(str(x))       # ✅ 5
```

`len()` 只能用在**有长度**的东西上：字符串、列表、字典……
**整数、小数、bool 都没有长度。**

**我在这题上错了两次，而且每次各对一半：**

| 时间 | 我写的 | 缺了什么 |
|---|---|---|
| 09-23 | `print(str(x))` | 缺 `len` |
| 09-25 | `print(len(x))` | 缺 `str` |

**正确写法：两个都要。** 先 `str` 转成字符串，再 `len` 数长度。

---

## 💡 想通了的概念

一开始想不通、后来搞明白的。复习时快速过一遍。

### Q1 · `True` 就是 `1`，那 `bool` 类型有什么用？

`1` 表达的是**数量**，`True` 表达的是**真假**。数值相等，意图完全不同。

**三个实际用途：**

**① 控制流程**——`if` / `while` 要的就是一个真假值
```python
if age >= 18:      # age >= 18 返回 True
    print("成年")
```
（你写 `if 1:` 也能跑，但三个月后自己都忘了 `1` 是什么意思）

**② 函数返回值**——行业惯例：`is_` / `has_` / `can_` 开头的函数返回 bool
```python
def is_even(n):
    return n % 2 == 0
```

**③ 能直接求和**——这是允许它等于 `1` 的实用理由
```python
sum([True, False, True, True, False])     # 3   ← 数出「有几个为真」
```

**最直接的证据**：`print(True)` 显示 `True`，`print(1)` 显示 `1`。
如果 bool 没意义，直接显示 `1` 就行了。

> Python 的选择：**有专门的类型（有语义），但底层继承 `int`（好用）**。两头都占。

### Q2 · 「是假值」和「等于 0」为什么是两回事？

见 [M3](#m3--以为-none--0-是-true)。简单说：
**「假」是判断的结果，「等于 0」是数值比较的结果**——两个不同的比较，自然可以答案不同。

---

## 🎯 自测题（复习日用）

**范围：第 1-2 课**

> 规则：**不看上面的内容**，用纸或另开文件写。写完再回来对照。

1. `x = 1` 然后写 `x = "hello"`，会报错吗？为什么？
2. `"24" + "1"` 的结果是什么？是数字还是字符串？
3. 想知道一个整数有几位，代码怎么写？
4. `True == 1` 返回什么？`True + True` 等于几？
5. `None == 0` 是什么？`bool(None)` 是什么？这两个答案一起说明了什么？
6. `0.1 + 0.2` 等于 `0.3` 吗？为什么？
7. `int(3.99)` 和 `round(3.99)` 分别是多少？
8. 写一行 f-string，把变量 `name` 和 `age` 拼进一句话。
9. `print(123)` 和 `print("123")` 输出有区别吗？那怎么区分它们？
10. 写代码前发现要用 `math.factorial()`，第一步该做什么？

---

## 📅 更新记录

| 日期 | 更新内容 |
|---|---|
| 2026-09-24 | 建立笔记，录入第 1-2 课（6 个坑、11 条 C 差异、8 个想错的地方、10 道自测题） |
| 2026-09-24 | 巩固第 2 课：强化 M2/M3（附亲手跑出的证据表）、新增 M9/M10、「💡 想通了的概念」章节收录 bool 的用途 |
